#!/usr/bin/env ruby

require 'yaml'
require 'shoryuken'

config = YAML.load File.read(File.join(File.expand_path('../..', __FILE__), 'shoryuken.yml'))

AWS.config(config['aws'])

10.times do |i|
  Shoryuken::Client.queue_by_name('shoryuken').send_message("Yo #{i}")
end
