aws:
  access_key_id:      <%= ENV['AWS_ACCESS_KEY_ID'] %>
  secret_access_key:  <%= ENV['AWS_SECRET_ACCESS_KEY'] %>
  region:             us-east-1
  receive_message:
    attributes:
      - receive_count
      - sent_at
queues:
  - my_queue1
  - my_queue2
